<p style="color: green"><%= notice %></p>

<h1>Customers</h1>

<%= link_to "Add new customer +", new_customer_path, :class => 'btn btn-light' %>

<div id="customers" class="d-flex gap-2 flex-wrap">
  <% @customers.each do |customer| %>
  <%= render customer %>
  <% end %>
</div>